/**
 * ChatLargeModal - Stub component for desktop app
 *
 * The full chat modal is not needed for POS Desktop.
 * This stub prevents import errors while the app loads.
 */

export function ChatLargeModal() {
  // No-op - chat is not needed for POS Desktop
  return null;
}

export default ChatLargeModal;
